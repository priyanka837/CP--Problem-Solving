#include <bits/stdc++.h>
using namespace std;

int main() {
    int t;
    cin >> t;
    while (t--) {
        int n;
        string s;
        cin >> n >> s;
        set<char> solved;
        for (char c : s) solved.insert(c);
        cout << n + solved.size() << "\n";
    }
}
